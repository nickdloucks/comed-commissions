import jasmine from "jasmine";
import { default as adminRouter } from 'admin-dash.js';

describe("The middleware for authenticating an admin user.", ()=> {
    it("Verifies the user correectly.", ()=>{

    });
    
    it("Verifies the password correctly", ()=>{

    });

    it("Does not allow access with password logic flaws.", ()=>{

    });
});